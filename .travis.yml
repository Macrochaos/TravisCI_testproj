

language: cpp
os: linux
compiler: gcc

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - gcc-7
      - g++-7
      - cmake

install:
 #gcc -Wall main.cpp -o test.out
 - cmake .

after_install:
 - .test.out
 

script:

 